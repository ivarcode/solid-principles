
public class MyClass {
    public void postComment() {
        if (ValidateUser()) {
            // post comment
        }
    }

    private ValidateUser(comment) {
        // check if user has valid credentials
    }
}